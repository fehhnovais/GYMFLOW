server {
  listen 80;

  location / {
    # redireciona as requisições para o container do Node
    proxy_pass http://nodeweb:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
  }
}